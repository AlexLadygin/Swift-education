//Tuples
let tuples  = (1, "hello", true ,2.4)

let (number, greatings, check, decimal) = tuples

number
greatings
check
decimal

tuples .0
tuples .1
tuples .2
tuples .3

let (_,_,check2,_) = tuples
check2

let tuple = (index:1, phrase: "hello", registered:true , latency:2.4)

tuple.phrase
tuple.index
tuple.latency
tuple.registered

/*
let a = (x:1, y:2)
var b = (x:2, y:3)

b = a
*/

/*
let redColor = "red"
let blueColor = "blue"
let greenColor = "green"
*/
let (red, blue, green) = ("red","blue", "green")

red
green
blue

let totalNumber = 5
let merchantName = "Alex"

print ((totalNumber),(merchantName))

